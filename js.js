/*const pakets = document.querySelectorAll('.paket')
for (const paket of pakets) {
paket.addEventListener('mouseover', () => {
    paket.classList.add('hover')
})
}
for (const paket of pakets) {
    paket.addEventListener('mouseleave', () => {
        paket.classList.remove('hover')
    })
    }

*/
